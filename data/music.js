/**
 * music.js — Music portfolio data.
 *
 * Each entry represents a work, album, or project.
 * Fields are a starting point — extend as needed.
 *
 * @type {Array<{
 *   id: string,
 *   title: string,
 *   year: number,
 *   description: string,
 *   tags: string[],
 *   embed?: string,       // SoundCloud / Bandcamp iframe src
 *   video?: string,       // YouTube / Vimeo iframe src
 *   audio?: string,       // path to self-hosted audio file
 *   cover?: string,       // path to cover image
 * }>}
 */
const music = [];

export default music;
