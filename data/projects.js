/**
 * projects.js — Miscellaneous shows, collaborations, and other projects.
 *
 * @type {Array<{
 *   id: string,
 *   title: string,
 *   year: number,
 *   description: string,
 *   link?: string,
 *   image?: string,
 *   tags: string[],
 * }>}
 */
const projects = [];

export default projects;
